<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h2 class="mb-1" style="font-size:1.1rem;font-weight:700;">Médicos</h2>
        <p class="text-muted fs-sm mb-0"><%= total %> registro(s)</p>
    </div>
    <% if (usuario && usuario.permissoes && usuario.permissoes.medicos && usuario.permissoes.medicos.criar) { %>
    <a href="/medicos/novo" class="btn btn-primary btn-sm" id="btn-novo-medico">
        <i class="bi bi-plus-lg me-1"></i>Novo Médico
    </a>
    <% } %>
</div>

<div class="card mb-3">
    <div class="card-body py-2">
        <form method="GET" action="/medicos" class="d-flex gap-2 search-form">
            <div class="input-group input-group-sm">
                <span class="input-group-text"><i class="bi bi-search"></i></span>
                <input type="text" name="busca" class="form-control" placeholder="Buscar por nome ou CRM..."
                       value="<%= busca || '' %>">
            </div>
            <button type="submit" class="btn btn-outline-primary btn-sm">Buscar</button>
            <% if (busca) { %><a href="/medicos" class="btn btn-outline-secondary btn-sm">Limpar</a><% } %>
        </form>
    </div>
</div>

<div class="card">
    <div class="table-responsive">
        <table class="table table-hover mb-0">
            <thead>
                <tr><th>#</th><th>Nome</th><th>CRM</th><th>Especialidade</th><th>Telefone</th><th>Status</th><th class="text-end">Ações</th></tr>
            </thead>
            <tbody>
                <% if (!registros.length) { %>
                <tr><td colspan="7" class="text-center text-muted py-4">
                    <i class="bi bi-inbox fs-4 d-block mb-2"></i>Nenhum médico encontrado.
                </td></tr>
                <% } %>
                <% registros.forEach(m => { %>
                <tr>
                    <td class="text-muted fs-sm"><%= m.id %></td>
                    <td><strong><%= m.nome %></strong></td>
                    <td class="fs-sm"><code><%= m.crm %></code></td>
                    <td class="fs-sm"><%= m.especialidade_nome || '—' %></td>
                    <td class="text-muted fs-sm"><%= m.telefone || '—' %></td>
                    <td>
                        <span class="badge <%= m.ativo ? 'badge-confirmada' : 'badge-cancelada' %> rounded-pill">
                            <%= m.ativo ? 'Ativo' : 'Inativo' %>
                        </span>
                    </td>
                    <td class="text-end">
                        <% if (usuario.permissoes.medicos && usuario.permissoes.medicos.editar) { %>
                        <a href="/medicos/<%= m.id %>/editar" class="btn btn-outline-primary btn-sm me-1">
                            <i class="bi bi-pencil"></i>
                        </a>
                        <% } %>
                        <% if (usuario.permissoes.medicos && usuario.permissoes.medicos.deletar) { %>
                        <form action="/medicos/<%= m.id %>/deletar" method="POST"
                              class="form-delete d-inline" data-nome="<%= m.nome %>">
                            <button type="submit" class="btn btn-outline-danger btn-sm">
                                <i class="bi bi-trash"></i>
                            </button>
                        </form>
                        <% } %>
                    </td>
                </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
</div>
<%- include('../partials/paginacao') %>